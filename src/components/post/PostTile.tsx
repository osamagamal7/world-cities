import React from "react";

type MainPostProps = {
    content: string;
    image_url: string;
    id: number;
    removePost: (id: number) => void;
    title: string;
};

export const PostTile: React.FC<MainPostProps> = ({

}) => {

    return (
        <></>
    );
};
